{
  "name": "shelfs",
  "version": "1.0.0",
  "description": "Personal cloud storage system",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
